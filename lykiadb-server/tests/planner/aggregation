#[name=single, run=plan]>

SELECT id FROM books b group by id;

---

- project [id as id]
  - aggregate [group_by=[id], aggregates=[]]
    - scan [books as b]

#[name=multiple, run=plan]>

SELECT release_year as yr FROM books b group by release_year, publisher_id;

---

- project [release_year as yr]
  - aggregate [group_by=[release_year, publisher_id], aggregates=[]]
    - scan [books as b]

#[name=incorrect_aggregate, run=plan]>
SELECT avg(1 + avg(id)) as average FROM books b;

---err

Plan(Err("Aggregate function cannot be nested inside another aggregate function"), Span { start: 7, end: 7, line: 0, line_end: 0 })

#[name=with_aggregate, run=plan]>
SELECT publisher_id, release_year as yr, avg(rating) as avg_rating FROM books b group by release_year, publisher_id order by yr;

---

- project [publisher_id as publisher_id, release_year as yr, avg(rating) as avg_rating]
  - aggregate [group_by=[release_year, publisher_id], aggregates=[avg(rating)]]
    - scan [books as b]


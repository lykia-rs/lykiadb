@group join_err {
    @set(run = "plan")

    @test illegal_subquery_in_condition {
        explain select * from books inner join categories ON (select * from books);

        @expect_err {
            Plan(SubqueryNotAllowed(Span { start: 0, end: 0, line: 0, line_end: 0 }))
        }
    }
}
